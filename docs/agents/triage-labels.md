# Triage labels

이 저장소는 다음 상태 어휘를 사용합니다.

- `needs-triage`: 유지보수자의 평가가 필요합니다.
- `needs-info`: 보고자 또는 사용자의 추가 정보가 필요합니다.
- `ready-for-agent`: 추가 맥락 없이 에이전트가 작업을 시작할 수 있습니다.
- `ready-for-human`: 사람의 구현 또는 판단이 필요합니다.
- `wontfix`: 진행하지 않기로 결정한 작업입니다.

로컬 이슈에서는 문서 frontmatter의 `status` 값으로 상태를 기록합니다.
