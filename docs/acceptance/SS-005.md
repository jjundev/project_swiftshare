# SS-005 acceptance evidence

## Automated command

```sh
make test
```

## Implemented

- Cross-language discovery advertisement, authenticated probe, endpoint QR,
  optional bootstrap ticket, availability reducer, and diagnostic contracts.
- Android ephemeral TLS listener, actual-port DNS-SD advertisement, rotating
  opaque instance/TXT metadata, isolated handshake/probe capacity, signed endpoint
  QR generation, and paired-only app-open availability.
- macOS Bonjour browsing, any-paired-pin identity probe, authenticated route
  aggregation, exact-pin route racing, endpoint QR camera/paste flow, and product
  UI cutover away from manual host/pin fields.
- Local-network/Bonjour/camera declarations and trust-first unpair invalidation.

## Required real-device observations

- Automatic Discovery followed by the existing one-file Mac-to-Android Transfer.
- Endpoint QR followed by the same authenticated Transfer with multicast disabled.
- IPv4-only, IPv6-only, dual-stack, partial route loss, address change, Wi-Fi
  loss/reconnect, sleep/wake, firewall, client isolation, and VPN evidence.
- Discovery latency from browse start through authenticated Peer resolution.

Hardware-dependent observations are not claimed by automated builds.
